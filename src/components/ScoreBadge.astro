---
export interface Props {
  score: number;
}
const { score } = Astro.props;
const rounded = Math.round(score);
---

{typeof score === "number" && (
  <div class={`score-badge score-outline-${rounded}`}>
  {score}/10
</div>

)}
