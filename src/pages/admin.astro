---
// Simple approach - let CMS find config automatically
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="robots" content="noindex" />
    <title>Content Manager</title>
  </head>
  <body>
    <!-- Use a known stable version -->
    <script src="https://unpkg.com/decap-cms@3.3.3/dist/decap-cms.js"></script>
    
    <!-- Load custom preview -->
    <script src="/admin/index.js"></script>
    
    <!-- Simple initialization with debugging -->
    <script>
      console.log('🚀 Admin page loaded');
      
      // OAuth message debugging
      window.addEventListener('message', (event) => {
        console.log('📨 Received message:', event.data);
        
        if (typeof event.data === 'string' && event.data.includes('authorization:github')) {
          console.log('🔐 OAuth message:', event.data);
        } else if (event.data?.type?.includes?.('authorization:github')) {
          console.log('🔐 OAuth object:', event.data);
        }
      });
      
      // Wait for CMS to load then initialize
      if (window.CMS) {
        console.log('✅ CMS available immediately');
        initializeCMS();
      } else {
        window.addEventListener('load', () => {
          console.log('🔄 Window loaded, checking for CMS...');
          if (window.CMS) {
            initializeCMS();
          } else {
            console.error('❌ CMS still not available');
          }
        });
      }
      
      function initializeCMS() {
        try {
          console.log('🎬 Starting CMS initialization...');
          // Let CMS automatically find and load config.yml
          window.CMS.init();
          console.log('✅ CMS.init() called successfully');
        } catch (error) {
          console.error('❌ CMS initialization failed:', error);
        }
      }
    </script>
  </body>
</html>